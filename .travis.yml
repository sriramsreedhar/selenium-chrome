language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  # PyPy versions
  - "pypy"  # PyPy2 2.5.0
  - "pypy3" # Pypy3 2.4.0
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: pytest
YAML
install:
  - docker build -t walmart .
  - docker run -d -p 127.0.0.1:80:80 --name walmart walmart
